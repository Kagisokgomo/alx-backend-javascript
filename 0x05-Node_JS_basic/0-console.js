// 0-console.js
function displayMessage(message) {
  console.log(message);  // prints the message to the console
}

module.exports = displayMessage;  // export the function so it can be used in other files
